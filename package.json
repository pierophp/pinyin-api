{
  "private": true,
  "name": "pinyin-api",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --outfile=dist/index.mjs --minify --format=esm ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Piero Giusti <pierophp@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "itty-router": "^2.4.4",
    "serverless-cloudflare-workers": "^1.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.0.0",
    "@types/jest": "^26.0.23",
    "esbuild": "^0.13.10",
    "isomorphic-fetch": "^3.0.0",
    "jest": "^27.2.5",
    "prettier": "^1.18.2",
    "prisma": "^3.5.0",
    "ts-jest": "^27.0.5",
    "tslib": "^2.2.0",
    "typescript": "^4.2.4"
  }
}
